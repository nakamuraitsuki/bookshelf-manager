import React from "react";
import styles from "./Add.module.css"

const Add = () => {
    return (
        <div className={styles.Title}>
            <h1>Add page</h1>
        </div>
    );
};

export default Add;